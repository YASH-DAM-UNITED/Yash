import{useState}from"react";import{ChevronLeft,ChevronRight,ArrowUpRight}from"lucide-react";import{projects}from"../data/portfolio";export default function Projects(){const[a,setA]=useState(1);const delta=i=>{let d=i-a;if(d>2)d-=projects.length;if(d<-2)d+=projects.length;return d};return <section id="projects" className="section projectSec"><div className="shell reveal"><header><b>03</b><span>FEATURED PROJECTS</span></header><h2>Projects That Make an <em>Impact</em></h2><p>A showcase of my work in automation, data management, web applications and operational systems.</p><div className="projectStage"><button onClick={()=>setA((a-1+projects.length)%projects.length)}><ChevronLeft/></button><div className="cards">{projects.map((p,i)=>{let d=delta(i);return <article key={p[0]} className={d===0?"active":""} style={{"--x":d,opacity:Math.abs(d)>2?0:1}} onClick={()=>setA(i)}><div className="projectVisual"><span>0{i+1}</span><div className="miniGrid"/></div><h3>{p[0]}</h3><p>{p[1]}</p><div>{p[2].map(t=><small key={t}>{t}</small>)}</div>{d===0&&<a target="_blank" href="https://github.com/YASH-DAM-UNITED">View Project <ArrowUpRight/></a>}</article>})}</div><button onClick={()=>setA((a+1)%projects.length)}><ChevronRight/></button></div></div></section>}