import{useState}from"react";import{ChevronLeft,ChevronRight,ArrowUpRight}from"lucide-react";import SectionHead from"./SectionHead";import ProjectVisual from"./ProjectVisual";import{projects}from"../data/content";
export default function Projects(){const[a,setA]=useState(0);const d=i=>{let x=i-a;if(x>2)x-=projects.length;if(x<-2)x+=projects.length;return x};return <section id="projects" className="section projects"><div className="giantWord">PROJECTS</div><div className="shell reveal"><SectionHead n="03" label="FEATURED PROJECTS"/><div className="centerTitle"><h2>Systems That Create<br/><em>Real Impact.</em></h2><p>Not just UI concepts — workflows designed around actual operational problems.</p></div>
<div className="carousel"><button onClick={()=>setA((a-1+projects.length)%projects.length)}><ChevronLeft/></button><div className="deck">{projects.map((p,i)=>{let x=d(i);return <article className={x===0?"active":""} style={{"--x":x,"--abs":Math.abs(x)}} onClick={()=>setA(i)} key={p.title}><ProjectVisual type={p.visual} index={i}/><small>{p.type}</small><h3>{p.title}</h3><p>{p.desc}</p><div className="chips">{p.tags.map(t=><i key={t}>{t}</i>)}</div>{x===0&&<a target="_blank" href="https://github.com/YASH-DAM-UNITED">Explore on GitHub <ArrowUpRight/></a>}</article>})}</div><button onClick={()=>setA((a+1)%projects.length)}><ChevronRight/></button></div>
<div className="dots">{projects.map((_,i)=><i onClick={()=>setA(i)} className={i===a?"on":""} key={i}/>)}</div></div></section>}